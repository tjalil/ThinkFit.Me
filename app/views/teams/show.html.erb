<h1><%= @team.name %></h1>
<p>
  <%= @team.description %>
</p>


<div class="roster">
  <h3>Members</h3>
  
    <p> <%= render 'new_member' %> </p>
</div>

<div id="teamwall">
	<h1>Wall: </h1>
	<%= render "/comments/new" %>
	<%= render "/comments/each" %>
</div>
<%= paginate @comments %>

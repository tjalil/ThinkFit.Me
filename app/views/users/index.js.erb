// Fired and returned with a search getScript request
<% if @users %>
  $('#search-answer-user').html("<%= j(render @users) %>");
<% elsif @teams %>
  $('#search-answer-team').html("<%= j(render @teams) %>");
<% end %>
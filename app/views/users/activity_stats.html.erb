<section class="widget chat companywall">
  <header>
    <legend><h4>
      Activity Log History
    </h4></legend>
    <div class="body pre-scrollable">
      <div id="chat" class="chat">
        <div id="stat-message" class="stats">
        <ul>
          <% current_user.goals.each do |goal| %>
          <li><%= goal.name_of_activity_through_goal %></li><br>
            <% goal.activity_logs.each do |log| %>
              <% if log == nil %>
              <% else %>
                <li> Points: <%= log.points %></li>
                <li> Logged: <%= log.created_at.to_s(:just_date) %></li>
                  <% if log == goal.activity_logs.last %>
                  <%= button_to "Remove Log", user_goal_activity_log_path(current_user, goal, log.id), :class => 'btn btn-danger', method: :delete %><br>
                  <% end %>
              <% end %>
            <% end %>
          <% end %>
        </ul>
        </div>
      </div>
    </div>
  </header>
</section>
